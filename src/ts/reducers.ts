import { SELECT_COLOR } from './actions';

let defaultColor = new Color(40, 110, 170);

function selectedColor(state = defaultColor, action) {
  switch (action.type) {
      
    default:
      return state;
  }
}
